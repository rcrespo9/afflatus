 <h4><%= @photo_inspired.creation_title %></h4>
  	  <%= @photo_inspired.writing %>

  	<!-- <a href="/users/<%= @user.id %>/photo_inspireds/<%= @photo_inspired.id %>/edit">Edit</a> -->
  	<%= link_to("Back", user_path(@user)) %>
  	<%= link_to "Edit", edit_user_photo_inspired_path(@user) if current_user && @user.id == current_user.id %>
<div class="classical-music-show container">
	<h1 class="text-center"><%= @music_inspired.creation_title %></h4>
	<h3 class="text-center">by <%= @music_inspired.writer_name %></h3>
	<h4 class="text-center">Inspired by <%= @music_inspired.artist_name %>'s &quot;<%= @music_inspired.song_title %>&quot;</h4>
	<br>
	<br>
	<p class="text-center"><%= @music_inspired.writing %></p>
	<br>
	<br>
	<p class="text-center">
	<%= link_to("Back", user_path(@user)) %>&nbsp;&nbsp;&nbsp;&nbsp;
	<%= link_to "Edit", edit_user_music_inspired_path(@user) if current_user && @user.id == current_user.id %>
	</p>
</div>
<br>
<%= image_tag @user.avatar.url %>
<h3>Username: <%= @user.username %></h3>
<h3>First Name: <%= @user.first_name %></h3><h3><%= @user.last_name %></h3>
<h3>Gender: <%= @user.gender %></h3>
<h3>Location: <%= @user.location %></h3>
About: <%= @user.about %>
<br>

<% if !@user.photo_inspireds.empty? %>
    <h2>Creations</h2>
    <% @user.photo_inspireds.each do |photo_inspired| %>
  <h4><%= link_to("#{photo_inspired.creation_title}", user_photo_inspired_path(@user)) %></h4>
<!--   <br> -->
<% end %>
<% end %>

<%= link_to "Edit Profile", edit_user_path(@user) if current_user && @user.id == current_user.id %>
<%= link_to "Create Photo Inspired", new_user_photo_inspired_path(@user) if current_user && @user.id == current_user.id %>

